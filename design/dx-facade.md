# DX facade (implemented in this checkout)

Public `Compile` → immutable `Plan`, `ScanWith`, `ScanPathsWith`, `EachFile`,
`WithLogger`, `WithProgress`, `Files`, `ScanIntoErr`, and typed byte limits
are in the working tree on top of commit `17040657`.

Closed in this checkout:

- optional `watch/` fsnotify module (never a main-module require)
- official B01–B14 first campaign (`MEASURED`, 1000-file tree)
- published library `v0.1.0` / CLI `cmd/treestamp/v0.1.1`

Do not copy RFC-only names into recipes until a test runs them.
